@page "/counter"

<PageTitle>Counter</PageTitle>

@*<CascadingValue Value="@currentUserIdentityForm">
    <div class="flex items-center justify-center">
        <div class="p-4 w-full max-w-sm bg-white rounded-lg border border-t-4 border-t-purple-700 shadow-md sm:p-6  dark:bg-gray-800 dark:border-gray-700">
            <h5 class="text-xl  font-medium text-center mb-6 text-gray-900 dark:text-white">میز خدمت الکترونیک</h5>
            @if (currentIdentityFormStep == UserIdentityFormStep.SignUp)
            {
                <IdentityForm OnFormSubmitted="UserFormSubmitted" />
            }
            else if (currentIdentityFormStep == UserIdentityFormStep.Services)
            {
                <Services />
                <div class="flex justify-center mt-2">
                    <button @onclick="(() => currentIdentityFormStep = UserIdentityFormStep.SignUp)" type="submit" class="w-full text-white bg-purple-700 hover:bg-purple-800 focus:ring-4 focus:outline-none focus:ring-purple-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-purple-600 dark:hover:bg-purple-700 dark:focus:ring-purple-800">
                        برگشت به صفحه اصلی
                    </button>
                </div>
            }
        </div>

    </div>
</CascadingValue>*@

@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }
}
